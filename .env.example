# Database
DATABASE_URL="postgresql://solarops:solarops@localhost:5432/solarops?schema=public"

# Auth.js / NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me-to-a-long-random-secret"

# Reminder cron endpoint auth (optional but recommended)
CRON_SECRET="dev-cron-secret"

# SMTP for production (optional in dev; app logs emails to console when omitted)
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@solarops.local"

# Optional feature flag (not enabled in MVP)
SMS_ENABLED="false"
